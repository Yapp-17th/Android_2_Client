<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sport_planet">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".presentation.login.GlobalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".presentation.chatting.view.ChattingActivity"
            android:theme="@style/ChattingAppTheme"
            android:windowSoftInputMode="adjustResize"></activity>
        <activity android:name=".presentation.filter.FilterActivity"></activity>
        <activity android:name=".presentation.profile.ExerciseDialog"
            tools:ignore="Instantiatable"></activity>
        <activity android:name=".presentation.profile.ProfileActivity"></activity>
        <activity android:name=".presentation.mypage.history.HistoryActivity"/>
        <activity android:name=".presentation.login.LoginActivity" />
        <activity android:name=".presentation.main.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.splash.IntroActivity" />
        <activity android:name=".presentation.splash.SplashActivity" />
        <activity android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="oauth"
                    android:scheme="kakao{27b6df70cdf8f39d98b68da14b2650bd}" />
            </intent-filter>
        </activity>
    </application>

</manifest>